import React from 'react';
import { ContainerBackground, Left, Right } from './styles';

const Background: React.FC = () => (
    <ContainerBackground>
        <Left />
        <Right />
    </ContainerBackground>
  );
  
  
  export default Background;